# TradingAgents 香港服务器部署报告

> 日期：2026-07-27 | 服务器：雨云香港 64.90.30.148 (Debian 12) | 变更编号：#010

---

## 一、部署结果

| 项目 | 状态 | 地址 |
|------|:--:|------|
| 前端页面 | ✅ | https://ngxago.top/trading/ |
| 后端 API | ✅ | https://ngxago.top/api/health → `{"status":"ok"}` |
| 简历首页 | ✅ | https://ngxago.top/ (不受影响) |
| PM2 守护 | ✅ | `trading-api` online, 重启自动恢复 |

---

## 二、遇到的 3 个坑及解决方案

### 坑 1：PM2 无法启动 Python venv (🟡 中)

**现象**：`pm2 start 'source venv/bin/activate && python server/main.py'` 反复崩溃，restart 15 次

**根因**：PM2 用 `/bin/sh`（非 bash）执行命令，`source` 在 sh 下不支持。且 `--interpreter` 参数对 shell 管道无效

**尝试过的失败方案**：
- `pm2 start ... --interpreter bash` — 仍然 sh
- `pm2 start ... --interpreter python3` — 忽略，走 bash

**最终方案**：写 `start.sh` 包装脚本，PM2 直接执行脚本文件
```bash
#!/bin/bash
cd /home/fde/trading-server
export PYTHONIOENCODING=utf-8
exec venv/bin/python server/main.py
```
```bash
pm2 start start.sh --name trading-api
```

---

### 坑 2：Vite 构建资源路径错误 (🟡 中)

**现象**：页面白屏，JS/CSS 返回 404

**根因**：Vite 默认 `base: '/'`，打包后 HTML 引用 `/assets/x.js`。但前端部署在 `/trading/` 子路径下，文件实际在 `/trading/assets/x.js`

**检测方法**：
```bash
curl -s https://ngxago.top/trading/ | grep 'src=\|href='
# 输出：src="/assets/index-xxx.js"  ← 缺少 /trading 前缀！
```

**解决方案**：
1. `vite.config.js` 加 `base: '/trading/'`
2. 服务器重构建：`npx vite build --base /trading/`
3. HTML 变为 `<script src="/trading/assets/x.js">` ✅

---

### 坑 3：nginx 正则 location 拦截静态资源 (🔴 大，卡最久)

**现象**：页面 HTML 200 ✅，但 JS/CSS 文件全部 404 ❌

```
HTTP 200  https://ngxago.top/trading/           ← HTML 正常
HTTP 404  https://ngxago.top/trading/assets/x.js  ← 静态资源 404
```

**排查过程（4 轮）**：

| 轮次 | 假设 | 验证 | 结论 |
|:--:|------|------|------|
| 1 | sed 把配置粘成一行 | `grep` 检查 | ❌ 已排除（手动重写了配置） |
| 2 | `alias` 和 `try_files` 冲突 | 改用 `root` | ❌ 仍然 404 |
| 3 | 文件权限问题 | `ls -la` 检查 | ❌ 文件存在且可读 |
| 4 | **nginx location 匹配优先级** | 检查完整配置 | ✅ **命中！** |

**真正的根因**：宝塔默认配置中有全局正则 location 匹配 `.js` / `.css` 文件：

```nginx
# 宝塔默认配置 — 比前缀 location 优先级更高！
location ~ .*\.(js|css)?$ {
    expires      12h;
    # 没有 root 指令 → 继承 server 级 root /www/wwwroot/resume
    # 实际查找 /www/wwwroot/resume/trading/assets/x.js → 404！
}
```

nginx location 优先级规则：**正则匹配 `~` > 前缀匹配（无修饰符）**

`/trading/assets/x.js` 被正则 `~ .*\.(js|css)` 截获，用 `root /www/wwwroot/resume` 去查 `/www/wwwroot/resume/trading/assets/...`——这个路径不存在。

**最终方案**：`location /trading/` → `location ^~ /trading/`

`^~` 修饰符 = "前缀优先，匹配后不再检查正则"。这是 nginx 文档中明确的最佳实践：如果某个前缀路径需要独立 root，必须用 `^~` 防止被正则 location 拦截。

```nginx
location ^~ /trading/ {
    root /www/wwwroot;
    try_files $uri $uri/ /trading/index.html;
}
```

---

## 三、关键经验（FDE 知识点）

| # | 经验 |
|---|------|
| 1 | **PM2 启动 Python 不要用 inline command**，用脚本文件，100% 可靠 |
| 2 | **Vite 子路径部署必须设 `base`**，不只是 index.html 的问题，所有资源引用都会错 |
| 3 | **nginx location 优先级是终极陷阱**：正则 `~` > 前缀（无修饰符）> `^~`。宝塔/lnmp 的默认 js/css 规则会拦截子路径的静态资源 |
| 4 | **排查 404 不要只看文件是否存在**——先确认 nginx 在哪个 location 处理的请求，`grep location` 全局审查 |
| 5 | **宝塔 nginx 默认配置有 js/css 正则 location**——以后部署子路径应用时第一件事检查会不会被拦截 |

---

## 四、当前服务器配置清单

```
/home/fde/trading-server/     ← 项目代码
/www/wwwroot/trading/          ← 前端静态文件
/www/wwwroot/resume/           ← 简历（不变）
PM2: trading-api (online)      ← 后端守护进程
nginx: html_ngxago.top.conf    ← 含 ^~ /trading/ 和 ^~ /api/
```

---

> 张宇峰 · FDE 培训 Day 15 · 变更 #010 · 2026-07-27
