# TradingAgents 94K 复现变更日志

> 原则：每次修改都记录「改了什么、为什么、原来是什么、改后是什么」——方便视频演示时讲解。

---

## 变更记录

### #001 | 2026-07-27 15:25 | 仓库克隆

- **操作**：通过 `gh-proxy.com` 镜像克隆 `TauricResearch/TradingAgents` (v0.3.1, ⭐94K)
- **原因**：GitHub 直连超时
- **命令**：`git clone https://gh-proxy.com/https://github.com/TauricResearch/TradingAgents.git`
- **目标路径**：`D:\423840203\423840203\github项目复现\TradingAgents 94k\`

---

### #002 | 2026-07-27 15:47 | 环境搭建

- **操作**：Python 3.11.5 venv + `pip install .` 安装所有依赖（60+ 包）
- **原因**：项目需要 langgraph, yfinance, langchain 等
- **关键依赖**：
  - `langgraph` 1.2.9 — Agent 工作流引擎
  - `yfinance` 1.5.2 — Yahoo Finance 数据
  - `langchain-openai` 1.4.1 — DeepSeek 兼容接口
  - `tradingagents` 0.3.1 — 本项目本体

---

### #003 | 2026-07-27 15:49 | LLM 模型名迁移

- **操作**：`.env` 中 `deepseek-chat` → `deepseek-v4-pro` + `deepseek-v4-flash`
- **原因**：DeepSeek 于 2026年7月24日 正式停用旧模型别名 `deepseek-chat` / `deepseek-reasoner`，今天(7/27)使用旧名会直接报错
- **原值**：
  ```
  TRADINGAGENTS_DEEP_THINK_LLM=deepseek-chat
  TRADINGAGENTS_QUICK_THINK_LLM=deepseek-chat
  ```
- **改后**：
  ```
  TRADINGAGENTS_DEEP_THINK_LLM=deepseek-v4-pro    # 分析师用：1.6T 参数，最强推理
  TRADINGAGENTS_QUICK_THINK_LLM=deepseek-v4-flash  # 风控用：284B 参数，快速经济
  ```
- **FDE 知识点**：生产环境的模型名会随时更新，拿到开源项目第一件事检查 API 兼容性

---

### #004 | 2026-07-27 15:30 | 配置中文输出

- **操作**：`.env` 设置 `TRADINGAGENTS_OUTPUT_LANGUAGE=Chinese`
- **原因**：默认英文报告，改为中文方便演示

---

### #005 | 2026-07-27 15:49 | 编码兼容

- **操作**：运行时需加 `PYTHONIOENCODING=utf-8`
- **原因**：Windows 中文环境下 Python 默认 GBK 编码，遇到 emoji 等 UTF-8 字符报 `UnicodeEncodeError`
- **命令**：`PYTHONIOENCODING=utf-8 python main.py`

---

### #006 | 2026-07-27 15:55 | 项目完整性检查

- **操作**：全面检查项目结构、依赖、功能可用性
- **检查结果**：
  - 源码：67 个 Python 文件，~12 万行代码
  - 模块完整性：agents / graph / dataflows / llm_clients / cli 五层架构齐全
  - 测试：53 个测试文件存在，pytest 未安装（dev 依赖可选）
  - 依赖：111 个包已安装，所有核心导入通过
  - 数据源：yfinance 因国内网络被限流（YFRateLimitError），Alpha Vantage / FRED 等未测试
  - 未跟踪文件：仅变更记录 一个，仓库代码未被修改
- **结论**：项目结构完整，可以直接使用；网络问题是外部因素，不影响代码学习

---

### #007 | 2026-07-27 16:00 | 项目架构分析（学习源码）

- **操作**：深入阅读核心模块，整理六层架构全景图
- **阅读文件**：
  - `tradingagents/graph/trading_graph.py` — 核心编排器
  - `tradingagents/graph/setup.py` — LangGraph 图构建
  - `tradingagents/dataflows/interface.py` — 多供应商路由
  - `tradingagents/graph/conditional_logic.py` — 条件路由
  - `tradingagents/llm_clients/openai_client.py` — LLM 提供商抽象层
  - `tradingagents/agents/analysts/market_analyst.py` — Agent 工厂模式示例
  - `tradingagents/agents/utils/agent_utils.py` — 工具函数 + 语言切换
  - `tradingagents/graph/propagation.py` — 状态初始化
  - `tradingagents/graph/reflection.py` — 反思/记忆机制
  - `tradingagents/graph/signal_processing.py` — 五档评级解析
  - `tradingagents/llm_clients/model_catalog.py` — 模型目录
  - `tradingagents/llm_clients/factory.py` — LLM 客户端工厂
  - `tradingagents/default_config.py` — 配置中心
- **架构总结**：
  ```
  CLI → graph(LangGraph) → agents(工厂+prompt+tools) → dataflows(多供应商路由) → LLM(15+提供商)
  ```
  核心流程：4 分析师 → 多空辩论 → 交易员 → 三方风控辩论 → 组合经理拍板
- **学习建议**：按 trading_graph → setup → conditional_logic → market_analyst → interface 顺序阅读

---

### #008 | 2026-07-27 16:05 | 记忆恢复 + 学习方式确认

- **操作**：加载了 identity.md、conversation-style.md、china-network.md、key-technical-decisions.md 四条记忆
- **确认**：宇峰选择"快速走读架构"方式学习此项目

---

### #009 | 2026-07-27 16:20 | Git Bash 写入权限问题

- **问题**：Git Bash 无法在 D 盘项目目录中创建/修改文件（Permission denied），Write/Edit 工具同样失败
- **原因**：Git Bash 的 POSIX 模拟层与 Windows 安全策略冲突
- **解决**：变更记录改存到 C 盘 Desktop 路径（`C:\Users\HP\Desktop\fde\day.15 github上找项目\变更记录.md`）
- **FDE 知识点**：Windows 环境下跨平台工具的文件操作不是 100% 兼容，遇到权限问题用原生路径或 C 盘用户目录

---

---

### #010 | 2026-07-27 16:35 | Alpha Vantage 免费 API Key

- **操作**：领取 Alpha Vantage 免费 API Key `JI672OOFCV70ED8`（每天 25 次请求）
- **原因**：yfinance 被 Yahoo 429 限流（国内共享出口 IP 问题）

---

### #011 | 2026-07-27 16:38 | 切换数据供应商为 Alpha Vantage

- **操作 1**：`.env` 添加 `ALPHA_VANTAGE_API_KEY=JI672OOFCV70ED8`
- **操作 2**：`default_config.py` 的 `data_vendors` 四类全部改为 `"alpha_vantage,yfinance"`（Alpha Vantage 优先，yfinance 兜底）
- **原值**：全部为 `"yfinance"`
- **改后**：`"alpha_vantage,yfinance"`
- **验证**：`get_stock('AAPL', '2024-10-01', '2024-11-01')` 返回 24 行 OHLCV，正常 ✅

---

### #012 | 2026-07-27 16:40 | 修复 Alpha Vantage 端点兼容性

- **操作**：`tradingagents/dataflows/alpha_vantage_stock.py` 第 38 行
- **问题**：`TIME_SERIES_DAILY_ADJUSTED` 是付费版端点，免费版调用报 `premium endpoint` 错误
- **改后**：改为 `TIME_SERIES_DAILY`（免费版可用，不复权数据，对学习/技术分析无影响）
- **FDE 知识点**：调用第三方 API 前先查文档确认免费/付费端点边界；开源项目的配置可能基于付费账号写成，需要适配

---

## 待做变更（后续复现时记录）

- [ ] ~~解决网络问题~~ → 已通过 Alpha Vantage 解决 ✅
- [ ] 跑通首次完整分析（选择一个标的 + 日期）
- [ ] 改为分析 A 股 / 港股
- [ ] 调整 Agent 参数（辩论回合数、温度）
- [ ] 每个模块深入学习
- [ ] 添加自己优化 / 新功能
- [ ] 视频录制

---

---

### #017 | 2026-07-27 17:30 | 子 Agent 卡住根因分析

- **现象**：T1 用 haiku 模型派发，10 分钟未产出报告文件
- **根因**：
  1. **模型选太弱**：haiku 读 529 行的 `trading_graph.py` + 理解插入点 + 处理 D 盘权限限制，超过了它的有效处理能力
  2. **指令矛盾**："读 D 盘文件但写到 C 盘"这种异常约束让模型原地打转
  3. **任务不适合派发**：单文件加 40 行代码、插入位置明确，直接上手比派 Agent 快 10 倍
- **纠正措施**：
  - 简单机械改动（<50 行、1 个文件）直接写，不派发
  - 需要读大文件+理解上下文的任务用 sonnet 起步，不用 haiku
  - 子 Agent 指令只给一条清晰路径，不给"你可以 A 或 B 或 C"
- **FDE 知识点**：工具选择影响效率——不是所有任务都适合多 Agent 编排；小改动直接做比协调 Agent 更快

---

### #018 | 2026-07-27 18:00 | Phase 1 代码全部完成

- **产出**：trading_graph_patch.py + server/ + frontend/，React 构建通过
- **C 盘工作路径**：`C:\Users\HP\Desktop\fde\day.15 github上找项目\项目修改文件\`
- **待办**：管理员权限移到 D 盘 → 启动后端 → 前端联调

---

## 未来 Phase 规划

- [ ] **Phase 2**: 国内 MCP 数据源（东方财富 / 同花顺）→ 支持 A 股分析
- [ ] **Phase 3**: 报告卡片增强 + 历史记录完善
- [ ] **Phase 4**: UI 打磨 + 部署香港服务器

---

### #007 | 2026-07-27 22:15 | 安装中国股票数据源

- **操作**：`pip install akshare`（VS Code 小克建议的 `china-market-data` 在 PyPI 不存在，akshare 是实际可用的等效库）
- **原因**：TradingAgents 默认只支持 yfinance（美股），需要 akshare 补充 A 股/港股数据
- **akshare 能力**：A 股行情、财务数据、新闻、港股、期货，免费无需 API Key

---

### #008 | 2026-07-27 22:20 | 执行迁移清单（12任务全部完成）

- **操作**：按 `迁移执行清单.md` 逐项执行
- **结果**：12/12 ✅
  - 任务 1-6：复制 server + frontend + .env + eastmoney.py + 4个Python文件 + check_setup.py
  - 任务 7：interface.py 注册 eastmoney（导入+VENDOR_LIST+VENDOR_METHODS 三处）
  - 任务 8：default_config.py 东财优先（core_stock/indicator/fundamental 三行）
  - 任务 9：pip install fastapi uvicorn sse-starlette akshare
  - 任务 10：check_setup.py — 14/15 ✅（akshare 连接东方财富 ⚠️ 网络波动）
  - 任务 11：server/main.py 启动成功，/api/health → {"status":"ok"}
  - 任务 12：npm install + npm run build — dist/ 构建成功（429ms）
- **修复**：default_config.py 第136行缺逗号 → 语法错误导致 LLM 模块加载失败，加逗号修复

---

### #010 | 2026-07-27 23:55 | 服务器部署完成 + 3个关键坑

- **操作**：按 `服务器部署清单.md` 10 任务执行，部署到 64.90.30.148
- **结果**：https://ngxago.top/trading/ ✅ 上线
- **遇到的 3 个坑**：
  1. PM2 不兼容 `source venv/bin/activate` → 改用 start.sh 脚本
  2. Vite base 路径未设 → HTML 引用 `/assets/x.js` 而非 `/trading/assets/x.js`
  3. **nginx 正则 location 截获 js/css** → `^~` 前缀优先解决（卡最久）
- **产出**：`服务器部署报告.md`（完整故障分析 + FDE 经验总结）

---

### #011 | 2026-07-28 00:30 | A股数据源优化 + yfinance绕过

- **操作**：移除 yfinance/Reddit/StockTwits/FRED，全部加 A 股快速跳过
- **原因**：A股代码传给美股源每次超时拖死全流程
- **改动**：
  - `default_config.py`：vendor chains 移除 yfinance + FRED

---

### #012 | 2026-07-28 01:00 | 变更记录补档

- **前端设计**：腾讯蓝 Swiss（#FFFFFF + #1677FF + Inter），三栏布局，SSE 流式
- **东财适配器**：eastmoney.py ~350行，get_stock/get_fundamentals/get_indicator
- **代码原则**：dev-conventions §4.5 禁止截断/硬编码/吞错误
- **Phase 2 规划**：国内 MCP 数据源 → A 股分析 → UI 打磨
  - `stocktwits.py`：`fetch_stocktwits_messages` A股秒回跳过
  - `reddit.py`：`fetch_reddit_posts` A股秒回跳过  
  - `trading_graph.py`：`resolve_instrument_context` A股跳过yfinance
  - `trading_graph.py`：`_run_graph` 中 on_chunk 从仅 debug=True 改为 `debug or on_chunk`
- **测试结果**：Market Analyst ✅ → 流程跑到Portfolio Manager阶段，最后崩在 `ticker must be a non-empty string, got None`
- **明日待修**：None ticker bug + 服务端 FRED config 同步

---

> 张宇峰 · FDE 培训 Day 15 · 2026-07-27

---

# Day 16（2026-07-28）：A 股适配深度改造 + 并发修复

> 原则不变：每次修改都记录「改了什么、为什么、原来是什么、改后是什么」。

---

## 🐛 Bug 修复

### #019 | 2026-07-28 10:20 | `ticker must be a non-empty string, got None`

- **现象**：分析跑到 Portfolio Manager 阶段后崩
- **根因**：`propagate_stream()` 没设 `self.ticker`，对标 `propagate()` L384 漏了一行。`_log_state` 依赖 `self.ticker` 写结果文件
- **修复**：
  - 短期：`propagate_stream()` 加 `self.ticker = ticker`（对标 `propagate()`）
  - 根治：`_log_state(trade_date, final_state)` → `_log_state(ticker, trade_date, final_state)`，消除隐式契约。任何调用方不需要先设 `self.ticker`

### #020 | 2026-07-28 10:35 | `600667.SS` 不是有效的 A 股代码

- **现象**：LLM 在工具调用中给 A 股代码附加 `.SS` 后缀，东财适配器拒绝
- **根因**：`build_instrument_context` 输出 `preserving any exchange suffix (.TO/.L/.HK/.T)` 指令——这是给美股/港股的，但对 A 股是脏数据。LLM 照做后污染了东财数据召回
- **修复（三层防御）**：
  - 第一层 Prompt：A 股走独立上下文路径，明确说 "do NOT append .SS or .SZ"
  - 第二层 供应商路由：东财优先 → alpha_vantage/yfinance 降级
  - 第三层 适配器防御纵深：`_validate_a_symbol` 自动剥离 `.SS/.SZ/.SH` 后缀

### #021 | 2026-07-28 11:30 | `No available vendor for 'get_income_statement'`

- **现象**：Fundamentals Analyst 调 `get_income_statement` 时无可用供应商
- **根因**：A 股 vendor chain 移除了 yfinance，但东财未注册 `get_income_statement`/`get_balance_sheet`/`get_cashflow`
- **修复**：东财新增三个降级适配方法，返回提示引导 LLM 改用 `get_fundamentals`（ROE/PE/PB 等核心指标）

---

## 🏗️ 架构重构（邓文文标准）

### #022 | 2026-07-28 10:45 | 统一 `is_a_share()` 入口

- **问题**：A 股判断逻辑 `ticker.isdigit() and len(ticker) == 6` 散落在 4 个文件中重复
- **修复**：
  - `symbol_utils.py` 新增 `is_a_share(ticker)` + `a_share_exchange(ticker)` 作为唯一数据源
  - 4 处调用统一：`trading_graph.py`、`agent_utils.py`、`reddit.py`、`stocktwits.py`
- **邓文文原则**：单一数据源，配置驱动 —— 改一处全生效

### #023 | 2026-07-28 11:00 | 东财适配器扩展

- **新增 `get_news(symbol, start_date, end_date)`**：调用 akshare `stock_news_em`，拉取东方财富个股新闻（标题+内容+来源+时间），支持日期过滤。签名与 alpha_vantage/yfinance 的 `get_news` 一致
- **新增 `get_sentiment(symbol, curr_date)`**：调用 akshare `stock_individual_fund_flow`，拉取近 120 日逐日资金流向（主力/超大单/大单/中单/小单），输出结构化汇总+近 5 日明细+分析要点。作为 Reddit/StockTwits 的 A 股替代数据源
- **邓文文原则**：完整字段映射 + 类型转换 + 缺失处理，不截断

### #024 | 2026-07-28 11:10 | A 股意图分流

- **Sentiment Analyst**：检测 `is_a_share()` → 东财资金流向替代 Reddit/StockTwits；prompt 标签从 "StockTwits Bullish/Bearish" 切换为 "主力资金净流向"
- **News Analyst**：东财注册为 `get_news` 第一供应商（A 股命中，美股降级）
- **Vendor Chain 剪枝**：`prediction_markets` 关闭（Polymarket 美国政治事件对 A 股是噪声）；`macro_data` 留空（FRED 美国宏观数据不适用）

### #025 | 2026-07-28 11:50 | 并发架构修复

- **现象**：分析进行中（3-5 min LLM 推理），网站完全无响应
- **根因**：`propagate_stream` 是同步生成器，内部 `queue.Queue.get(timeout=0.5)` 阻塞 uvicorn 唯一的事件循环线程。分析期间事件循环被独占，无法处理任何新连接
- **修复**：
  - `server/main.py` 重构：同步 `propagate_stream` 丢入 `ThreadPoolExecutor`（max_workers=3）
  - `asyncio.Queue` + `call_soon_threadsafe` 桥接线程→事件循环
  - 事件循环始终保持空闲，health check / 第二个分析在分析进行中正常响应
- **原架构**：`浏览器 → uvicorn 事件循环 → propagate_stream (阻塞 3-5 min) → 其他请求排队`
- **改后架构**：`浏览器 → uvicorn 事件循环 → await event_queue (非阻塞) ← ThreadPoolExecutor ← propagate_stream`

---

## 📁 改动文件清单

| 文件 | 改动类型 | 说明 |
|------|:--:|------|
| `tradingagents/dataflows/symbol_utils.py` | 🆕 新增 | `is_a_share()` + `a_share_exchange()` 统一入口 |
| `tradingagents/dataflows/eastmoney.py` | 🆕 扩展 | `get_news()` + `get_sentiment()` + `_validate_a_symbol` 防御纵深 + 财报降级适配 ×3 |
| `tradingagents/dataflows/interface.py` | 注册 | 东财注册到 `get_news`/`get_income_statement`/`get_balance_sheet`/`get_cashflow` |
| `tradingagents/dataflows/reddit.py` | 重构 | 用统一 `is_a_share()` 替代内联判断 |
| `tradingagents/dataflows/stocktwits.py` | 重构 | 同上 |
| `tradingagents/graph/trading_graph.py` | 修复+重构 | `_log_state` 消除隐式契约 + `is_a_share()` 统一 |
| `tradingagents/agents/utils/agent_utils.py` | 重构 | A 股意图分流：`build_instrument_context` 不输出 exchange suffix 指令 |
| `tradingagents/agents/analysts/sentiment_analyst.py` | 🆕 扩展 | A 股路径：东财资金流向替代 Reddit/StockTwits，prompt 本地化 |
| `tradingagents/default_config.py` | 配置 | `news_data: "eastmoney,alpha_vantage"` + 关闭 `prediction_markets` |
| `server/main.py` | 🆕 重构 | 线程池+asyncio.Queue 并发架构 |

---

## 🧪 验证结果

| 测试 | 结果 |
|------|:--:|
| 600667 太极实业 完整分析 | ✅ 8 Agent 全部通过，最终决策：Underweight |
| `is_a_share()` 单元测试 | ✅ 7/7 |
| `_validate_a_symbol` 防御纵深 | ✅ .SS/.SZ/.SH 自动剥离 |
| 并发：分析中 health check | ✅ 立即响应（改前：超时） |
| 外网访问 | ✅ https://ngxago.top/trading/ 200 |

---

---

# Day 16 续（2026-07-28 下午）：Axon 品牌 + 前端重写 + 用户系统

### #026 | Axon 品牌重塑

- **命名**：TradingAgents → **Axon**（神经元轴突，多 Agent 信号传导）
- **URL**：ngxago.top/trading/ → **ngxago.top/axon/**
- **配色**：蓝 #1A73E8 → 青 #0EA5A0（神经元信号色）
- **设计风格**：工业终端 → 干净卡片化 Marvis 风格
- **README.md**：完整项目文档（中英双语，技术栈/对比/快速开始/API）

### #027 | AI 味道清除 + Markdown 渲染

- **Anti-AI-slop**（Hallmark 规范）：
  - 去掉所有 emoji 图标（📊💬📰🏢🐂🐻💹🎯）→ 等宽英文标签（MKT/SENT/NEWS/FUND/BULL/BEAR/TRD/PM）
  - 去掉彩色 badge 决策横幅 → 干净标题行
  - 去掉"最终决策"填充标签 → 内容即标题
- **Markdown 渲染**：marked 库，报告卡片直接渲染表格/标题/粗体
- **报告展开**：从点击展开改为默认全展开

### #028 | 用户系统

- **授权码**：3 大写字母 + 3 数字格式（如 MOZ308），白名单 `~/.axon/codes.txt`
- **管理员生成**：`POST /api/admin/gen-codes` 一键生成
- **数据隔离**：`~/.axon/{code}/` 每人独立目录
- **模型切换**：设置面板支持 DeepSeek/OpenAI/自定义
- **免费试用**：未配 Key 用户 3 次免费（兜底服务器 Key），管理员无限
- **自选股票**：搜索加星标，持久化到用户配置

### #029 | 前端功能增强

- **搜索框**：输入代码/名称即时筛选 39 只常见 A 股
- **Agent 迷你状态**：8 个小圆点实时显示进度
- **计时器 + 标语**：分析中顶栏显示 "2:35 多头和空头打起来了..."
- **双语切换**：中/EN 按钮，UI 框架层全双语
- **活动日志去 Markdown**：剥离 **### | 等格式符
- **移动端适配**：单页流式布局，安全区适配，iOS/Android 统一

### #030 | Bug 修复

- **内容截断**：`_extract_event` 和 API 返回去掉所有 `[:500]`/`[:800]`/`[:2000]`/`[:5000]` 硬截断
- **报告重复**：`agent_done` 去重（`seen_reports` set）
- **chunk 重复**：内容 hash 去重
- **计时 0:00**：`agentTimings` ref 防 React 重渲染覆盖
- **API Key 泄漏**：fallback 逻辑改为仅管理员+免费用户可用

### 📁 Day 16 新增/改动文件

| 文件 | 改动 |
|------|:--:|
| `server/main.py` | 🆕 用户系统 + 问答端点 + 管理端点 + 去除硬截断 |
| `frontend/src/App.jsx` | 重写：auth + 计时 + 去重 + Markdown 剥离 |
| `frontend/src/App.css` | 重写：Axon 品牌 + 移动端 + 统一尺寸 |
| `frontend/src/components/TopBar.jsx` | Axon 品牌 + 计时器 + 标语 + 设置按钮 |
| `frontend/src/components/Sidebar.jsx` | 搜索框 + 加星标 + 自选持久化 |
| `frontend/src/components/AgentCard.jsx` | 去 emoji，等宽标签 |
| `frontend/src/components/ReportPanel.jsx` | 全展开 |
| `frontend/src/components/ActivityFeed.jsx` | 🆕 替代 LogStream |
| `frontend/src/components/AskPanel.jsx` | 🆕 报告问答 |
| `frontend/src/components/AuthScreen.jsx` | 🆕 登录页 |
| `frontend/src/components/SettingsModal.jsx` | 🆕 设置面板 |
| `frontend/src/hooks/useAuth.js` | 🆕 认证 hook |
| `frontend/index.html` | Google Fonts 移除 + viewport 修复 |
| `frontend/vite.config.js` | base: `/trading/` → `/axon/` |
| `tradingagents/graph/trading_graph.py` | 去硬截断 + seen_reports 去重 |
| `tradingagents/agents/analysts/market_analyst.py` | 移除 get_verified_market_snapshot |
| `tradingagents/agents/analysts/sentiment_analyst.py` | A 股意图分流 + 日志 |
| `nginx` conf | `/trading/` → `/axon/` |

---

> 张宇峰 · FDE 培训 Day 16 · 2026-07-28
